// import express, { type Router } from "express";

// const router: Router = express.Router();

/*

routes for:
- Users (username, hashed password)
- Jobs (user creates their jobs to track time & rate)
- Projects (attached to jobs)
- Days (poject, hours_worked, cached_rate)

*/
